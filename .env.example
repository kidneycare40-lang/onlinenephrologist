# Database
DATABASE_URL="postgresql://user:password@localhost:5432/kidneycarecentre"

# NextAuth — generate random secrets: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="GENERATE_WITH: node -e console.log(require('crypto').randomBytes(64).toString('hex'))"

# OAuth Providers
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Razorpay
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
RAZORPAY_WEBHOOK_SECRET=""

# Supabase
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""
SUPABASE_SERVICE_ROLE_KEY="NEVER commit this — server-side only"

# JWT Secret — generate random: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="GENERATE_WITH: node -e console.log(require('crypto').randomBytes(64).toString('hex'))"

# Setup key for initial credential bootstrap (/api/auth/init)
SETUP_KEY="GENERATE_WITH: node -e console.log(require('crypto').randomBytes(32).toString('hex'))"

# OpenAI (server-side only — NEVER expose to browser)
OPENAI_API_KEY=""

# Email (Resend)
RESEND_API_KEY=""

# WhatsApp (CallMeBot — legacy fallback)
CALLMEBOT_API_KEY=""

# WhatsApp Cloud API (Meta Business)
WHATSAPP_VERIFY_TOKEN="GENERATE_WITH: node -e console.log(require('crypto').randomBytes(32).toString('hex'))"
WHATSAPP_ACCESS_TOKEN=""
WHATSAPP_PHONE_NUMBER_ID=""
WHATSAPP_BUSINESS_ACCOUNT_ID=""
WHATSAPP_API_VERSION="v21.0"
WHATSAPP_APP_SECRET=""

# WhatsApp Cloud API — optional
WHATSAPP_DOCTOR_PHONE_NUMBER=""
WHATSAPP_APPOINTMENT_TEMPLATE="appointment_confirmed"
WHATSAPP_DOCTOR_TEMPLATE="new_appointment_alert"

# Site
NEXT_PUBLIC_SITE_URL="https://www.onlinenephrologist.com"
NEXT_PUBLIC_SITE_NAME="Online Nephrologist"
NEXT_PUBLIC_WHATSAPP_NUMBER="919818235613"
NEXT_PUBLIC_EMERGENCY_NUMBER="919818235688"
